{
  "name": "agent-game-test",
  "private": true,
  "version": "0.1.0",
  "description": "A web-based Snake & Pong game — development workflow framework demo",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint src/ --ext .js || true",
    "test": "node tests/scaffold.test.js"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
